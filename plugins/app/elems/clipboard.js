export const init = ({ $app }) => {
  const clipboard = $app.clipboard = {}




  clipboard.copy = () => {
  }
  
  
  
  
  clipboard.paste = async (text) => {
  }
  
  
  
  
  clipboard.cut = () => {
  }
}